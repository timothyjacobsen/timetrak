<h1>New timeline</h1>

<% form_for(@timeline) do |f| %>
    <%= render :partial=>'edit', :locals => { :f=>f } %>
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', timelines_path %>